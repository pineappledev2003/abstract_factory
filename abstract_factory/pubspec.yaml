name: abstract_factory
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0-149.0.dev

dependencies:
  dio: ^5.8.0+1
  fluro: ^2.0.5
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  provider: ^6.1.4
  validatorless: ^1.2.4

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

assets:
  - "assets/"
